<h1>Post new image</h1>
<%= form_with model: @post_image do |f| %>
  <h4>image</h4>
  <%= f.file_field :image, accept: "image/*" %>
  <h4>Caption</h4>
  <%= f.text_area :caption %>
  <%= f.submit '投稿' %>
<% end %>